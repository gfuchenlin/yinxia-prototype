# 音匣 · 给 Cursor 的设计/产品交接（小杨整理）

> 用途：在 Cursor 里打开本文件（或 `@CURSOR-HANDOFF.md`），接上与 Grok Bot「小杨」侧已对齐的设计结论。  
> 更新：2026-09-18  
> 注意：**Grok Bot 与 Cursor 聊天默认互不同步**；以本文件 + 仓库/本地文件为准做桥接。

---

## 1. 项目是什么
- 产品名：**音匣**（自托管曲库 iOS 客户端）
- 对接：Navidrome / Subsonic 系；连接页亦覆盖 Jellyfin / Emby / Plex 等示意
- 角色分工（Grok Bot 侧）：小杨=UI/UX；小付=产品；小戴=iOS；小李=测试

## 2. 本地路径（用户 Mac `bogon`）
| 内容 | 路径 |
|---|---|
| 高保真可点原型 | `/Users/fuchenlin/Documents/tempwork/yinxia-prototype/` → 打开 `index.html` |
| iOS 工程 | `/Users/fuchenlin/Documents/tempwork/yinxia/`（含 `YinXia.xcodeproj`） |
| 设计 PNG / 标注（Grok Bot 箱内） | `/workspace/iphone-portrait/`（若需同步到本机可再拷） |

**约定：** 原型改动优先写本机 `yinxia-prototype`，避免反复下压缩包。

## 3. 已锁定的信息架构
### 3.1 启动链路（2026-09-18）
`启动页` → `选择音乐服务` → `连接配置` → `连接成功` → `曲库`  
（禁止启动后直达曲库）

### 3.2 底栏
`搜索` / `曲库` / `设置`（无独立「首页发现流」MVP）

### 3.3 曲库入口（2026-09-18）
排序（3 列，图标在上、文案在下）：`歌曲` → `我喜欢的` → `本地音乐` → `专辑` → `流派` → `歌手`。已去掉「最近」和独立「艺术家」入口（歌手宫格再进艺人详情）。

| 入口 | 图标 | 行为 |
|---|---|---|
| **歌曲** | `music.note` | 进入**全部歌曲**页；标题下不再写曲目数 |
| **我喜欢的**（原「收藏」） | `heart.fill` | 进入**歌单页**（智能歌单）；标题下不再写曲目数 |
| **本地音乐** | `iphone` | 进入本机曲库歌单页；标题下只展示**占用空间**（曲目数留给「全部播放」） |
| **专辑** | `square.stack.fill` | 专辑详情/列表 |
| **流派** | `guitars` | 先**风格宫格**（2 列大方块）→ 再进该流派歌曲歌单 |
| **歌手** | `person.fill` | 先**歌手宫格**（2 列大方块）→ 再进艺人详情 |
| **我的歌单**（原「播放列表」） | — | 曲库页下方列表，保持原逻辑 |

### 3.4 详情页（专辑/歌单/艺人）
- 进入后隐藏底栏 + 迷你播放条；返回恢复
- 头部：模糊封面、返回、标题、更多
- **全部播放（共 N 首）** + **页内搜索**
- 曲目副行：`格式徽章 + 体积 · 艺人 · 专辑`；在**专辑页省略专辑名**
- 艺人分段：`专辑` / `歌曲` / `相似`（相似是否进 MVP 待产品拍板；UI 可保留入口+空态）

### 3.5 歌曲「⋯」
- 列表行**不展示**心形/星级；只在 ⋯ 面板
- 心形 = 喜欢/取消；1–5 星 = setRating（0=未评）
- MVP 动作：下一首播放、追加队列、查看艺人、查看专辑  
- 后续灰显：下载、定时、倍速等

### 3.6 播放器
- 顶栏：收起 | **推荐 / 歌曲 / 歌词** | AirPlay
- 歌曲态：黑胶 + 唱臂（播放落针 / 暂停抬起）
- 底栏五键三态共用：循环 | 上一首 | 播停 | 下一首 | 队列
- 推荐：相似歌曲 + 热门歌曲（竖列表）；空态「暂无推荐」
- 歌词：可滚、当前行更大；无时间戳 UI；空态「暂无歌词」
- 歌曲页可有 3 行歌词预览，点进歌词 Tab

### 3.7 队列
- 竖屏底部弹层；横屏右侧栏
- 正在播放 + 接下来；拖拽排序；移除；清空需确认（会停播）
- 空态「队列为空」；点播仍追加到队列

## 4. 视觉与画布
- 竖屏稿：1170×2532（≈390×844 pt @3x）
- 横屏稿：2532×1170
- 品牌：Logo 方案 E；启动 T3；口号「装下你的每一首歌」
- 图标：实现侧一律 **SF Symbol**，不要切位图箭头
- 曲库入口：3 列大方块；SF Symbol 图标居中在上、文案同一字号居中在下
- 流派列表：2 列大方块（色块 + 风格名 + 曲目数），不再用单行列表

## 5. 原型说明
- 技术：每个屏幕一个 HTML（`pages/*.html`），共用 `shared.css` / `shared.js`。不再用 PNG 热区。
- 入口：`index.html` 页面目录，或 `pages/splash.html` 沿主路径点下去
- `assets/` 里的 PNG 仅作对照，页面不再引用
- 可见曲目按 6 首示意，「全部播放」与列表行数对齐（共 6 首）

## 6. 待产品拍板（实现可先按建议默认）
1. 相似艺人是否进 MVP → 建议：入口 + 空态
2. 全部播放：追加 vs 替换队列 → 建议：追加
3. 热门歌曲数据口径 → 建议：曲库播放次数 Top N
4. 横屏是否要「为你推荐」大卡 → 建议：MVP 不做

## 7. 给 Cursor / 小戴的实现优先级
1. 启动 → 连接（多服务）→ 曲库主路径  
2. 曲库六宫格新 IA（我喜欢的 / 歌曲 / 流派）  
3. 专辑·歌单·艺人详情 + ⋯ 面板  
4. 播放器三态 + 队列  
5. 横屏壳与播放分栏（见 `landscape-specs`）

## 8. 和 Grok Bot 小杨继续协作的方式
- 你在 Cursor 改代码/结构后：把结论写进本文件「变更记录」，或让改动落在上述本机目录  
- 回到 Grok Bot 时告诉小杨「看 CURSOR-HANDOFF / 本地原型」，即可续上  
- **不要假设** Cursor 对话会自动出现在小杨上下文里

## 9. 变更记录
| 日期 | 变更 |
|---|---|
| 2026-09-17 | 高保真 PNG 原型；播放/详情/队列规格锁定 |
| 2026-09-18 | 启动必须经连接；收藏→我喜欢的歌单；歌曲→全部歌曲；流派二级 |
| 2026-09-18 | 约定本机原型目录；生成本交接文件供 Cursor 使用 |
| 2026-09-18 | 曲库六宫格「我喜欢的」与「最近/艺人/专辑/歌曲/流派」统一左对齐、同一字号（不缩小、不居中） |
| 2026-09-18 | 流派页由单行列表改为 2 列大方块宫格 |
| 2026-09-18 | 曲库去掉「最近」；入口改为 歌曲 / 我喜欢的 / 本地音乐 / 专辑 / 艺术家 / 流派 / 歌手 |
| 2026-09-18 | 曲库入口改为 3 列并加 SF Symbol；去掉独立「艺术家」，保留「歌手」 |
| 2026-09-18 | 曲库下方「播放列表」改名为「我的歌单」 |
| 2026-09-18 | 本地音乐标题下只留占用空间，去掉与「共 24 首」重复的曲目数 |
| 2026-09-18 | 全部歌曲、我喜欢的标题下去掉与「共 24 首」重复的曲目数 |
| 2026-09-18 | 原型改为逐页 HTML，停用 PNG 热区 |

---

## 10. 协作实验协议（Cursor ↔ 小杨）

### 你怎么用
1. Cursor 打开文件夹：`Documents/tempwork/yinxia` 或 `yinxia-prototype`
2. 聊天里 `@CURSOR-HANDOFF.md`，并粘贴下方「短提示词」（见同目录 `CURSOR-PROMPT.md`）
3. Cursor 做完事后，**必须**往本文件 §11 回写区追加一条（日期 + 做了什么 + 改了哪些文件 + 待小杨确认的问题）
4. 回到 Grok Bot 跟小杨说：「看一下交接回写」——小杨读本机此文件后续聊

### 预期能达到的程度
| 能 | 不能 |
|---|---|
| 设计结论、路径、优先级被 Cursor 读到 | 两边聊天记录自动同步 |
| Cursor 改代码/原型后通过回写区告知小杨 | 小杨实时旁听 Cursor 对话 |
| 小杨按回写区继续改设计/原型 | 不写回文件就默认小杨已知晓 |

### 建议第一轮实验（15 分钟）
1. 在 Cursor 中确认启动链路与曲库三入口（我喜欢的 / 歌曲 / 流派）理解正确  
2. 对照 iOS 工程，标出「已实现 / 未实现 / 与交接不一致」三点，写入 §11  
3. 回到小杨这边，看小杨能否复述你的三点并给出设计跟进

---

## 11. Cursor 回写区（请追加，不要删旧记录）

<!-- Cursor 或你本人在每次会话结束后追加一条 -->

### 模板
```
#### YYYY-MM-DD HH:mm · Cursor 会话
- 做了：
- 改动文件：
- 与交接一致 / 有出入：
- 请小杨确认：
```

### 记录

#### 2026-09-20 08:50 · Cursor 会话（iOS P1）
- 做了：
  1. 选服务补 Plex；Jellyfin / Emby / Plex 点进「即将支持」，不假装已通。
  2. 连接失败改为 alert 卡 + 字段只读回显 + 重试 / 改用其他服务（对齐 CONNECT-SPEC §4）。
  3. 本地音乐改为三级详情页壳：副标题仅「占用 0 MB」，空态「暂无歌曲」；完整导入仍非 MVP。
  4. 空态/弱网条/加载失败重试；搜索空态文案对齐原型。
  5. 歌曲 ⋯：下一首播放、追加队列、查看歌手/专辑；心形/星级走 star / setRating。
  6. 详情页内搜索；歌手改为 2 列大方块；流派纯色左下文字；曲库宫格图标改为线框（heart / iphone / person）。
  7. 清掉 pbxproj 把 Swift 误挂进 Frameworks 的回归。
- 改动文件：`yinxia` 工程内 Auth / Library / Search / Settings / Player / AppCore / SubsonicAPI / `project.pbxproj`；本文件 §11。
- 与交接一致 / 有出入：P1 主路径按盖章落地。排序按钮仍是占位。连接页未做像素级截图验收。
- 请小杨确认：
  1. 选服务行「即将支持」小胶囊是否保留。
  2. 连接失败页是否需要居中短标题「连接 {服务}」（已做）。

#### 2026-09-18 11:10 · Cursor 会话（第一轮实验）
- 做了：
  1. 复述锁定 IA：启动必须 `启动页 → 选择音乐服务 → 连接配置 → 连接成功 → 曲库`；曲库三入口「我喜欢的」进智能歌单页、「歌曲」进全部歌曲歌单、「流派」先进风格列表再进该流派歌单。
  2. 对照本机高保真原型 `yinxia-prototype` 与 iOS 工程 `yinxia`（`YinXia.xcodeproj`），按「已实现 / 未实现 / 与交接不一致」盘点。
- 改动文件：仅本交接文件 §11（未改原型/未改 iOS）。
- 与交接一致 / 有出入：

  **已实现（原型侧，与交接一致）**
  - 启动链路热区：`splash → connect → connect-form → connect-success → library`，禁止直达曲库。
  - 曲库六宫格：我喜欢的 → `playlist-liked`；歌曲 → `playlist-all-songs`；流派 → `genre-list` → Pop/R&B 等歌单。
  - 底栏三键：搜索 / 曲库 / 设置；播放器推荐·歌曲·歌词；队列弹层；歌曲 ⋯ 面板（喜欢、下一首、追加、查看艺人/专辑）。
  - 艺人「相似」保留入口 + 空态屏。

  **已实现（iOS 工程，可当底座）**
  - Navidrome/Subsonic 登录（Keychain）+ ping；曲库专辑网格；专辑详情「播放全部」；搜索歌曲/专辑；独立歌单 Tab + 歌单详情；迷你条 + 全屏播放（封面/进度/上一下一首）；后台音频与锁屏控制；设置页口号「装下你的每一首歌」、Logo 方案 E。

  **未实现（iOS vs 交接）**
  - 启动四步：无启动页、无四服务选择（Jellyfin/Emby/Plex 示意）、无连接成功页。
  - 曲库新 IA：无「我喜欢的 / 歌曲 / 流派」入口，无风格列表、无全部歌曲页、无艺人详情（专辑/歌曲/相似）。
  - 详情规格：无模糊封面头、无「全部播放（共 N 首）」+ 页内搜索、无副行「格式徽章 + 体积 · 艺人 · 专辑」、无歌曲 ⋯（心形/星级/下一首/追加队列）。
  - 播放器三态：无推荐/歌词 Tab、无黑胶唱臂、无队列 UI（竖屏弹层/横屏侧栏）、无循环键、无横屏壳。

  **与交接不一致（iOS）**
  - 底栏：工程是 **曲库 / 搜索 / 歌单 / 设置** 四 Tab，且曲库在左一；交接是 **搜索 / 曲库 / 设置** 三 Tab、无独立歌单 Tab（播放列表应在曲库内）。
  - 启动：未登录时直接登录表单 → 登录成功直达 Tab；交接禁止启动后直达曲库，且要先选服务。
  - 曲库主界面：工程是「最新专辑网格」；交接是六宫格入口（最近 / 我喜欢的 / 艺人 / 专辑 / 歌曲 / 流派）+ 下方播放列表。
  - 专辑「播放全部」当前是替换队列开播；交接建议默认「追加」（待小付拍板）。

  **原型已知瑕疵（交接 §5 已记，仍在）**
  - 歌单标题数量（128/1024）与列表「共 24 首」示意数据未对齐。
- 请小杨确认：
  1. 下一轮 Cursor 是否按交接 §7 优先级改 iOS（先启动链路 + 曲库三入口），还是先修原型数据不一致？
  2. 底栏四 Tab（含独立「歌单」）是工程历史债，还是产品后来改过？Cursor 默认按三 Tab 收敛。
  3. 「全部播放」在 iOS 落地时，是否先按建议做「追加」而不是替换？

#### 2026-09-18 11:22 · Cursor 接替 UI（曲库六宫格字号）
- 做了：曲库页「我喜欢的」从「收藏」改名时被缩小并水平居中，与其余五入口不一致。已按「最近 / 专辑 / 流派」的左对齐与 46px 字高重绘，文案仍为「我喜欢的」。热区未改，点击仍进智能歌单页。
- 改动文件：
  - `yinxia-prototype/assets/library.png`（以 `_library_src.png` 为底只替换标签）
  - 本文件 §9 变更记录 + §11
- 与交接一致 / 有出入：IA 不变（我喜欢的 → 歌单页）。视觉规则补一条：**六宫格入口同一字号、左对齐；四字文案不缩小、不居中。**
- 请小杨确认：四字入口是否保持与两字同一字号（当前方案），还是允许略缩以更疏。默认维持同一字号。

#### 2026-09-18 13:10 · Cursor 接替 UI（流派 2 列大方块）
- 做了：流派进入后不再用单行列表。改为 **2 列大方块宫格**（8 个风格：Pop / R&B / Rock / Jazz / Electronic / Classical / Hip-Hop / Indie），色块 + 风格名 + 曲目数；热区按宫格重布。Pop / R&B 进对应歌单，其余示意进 Pop 歌单。
- 改动文件：
  - `assets/genre-list.png`（新宫格稿；旧列表稿留作 `assets/_genre-list_src.png`）
  - `app.js`（热区）
  - `README.md`、`IA-UPDATE-2026-09-18.md`、本文件 §3.3 / §4 / §9 / §11
- 与交接一致 / 有出入：二级路径不变（流派 → 风格 → 该流派歌单），只改展现。
- 请小杨确认：大方块用纯色 + 左下文字（当前），还是要补风格封面/纹理。

#### 2026-09-18 13:20 · Cursor 接替 UI（曲库入口重排）
- 做了：曲库去掉「最近」。入口改为 **歌曲 / 我喜欢的 / 本地音乐 / 专辑 / 艺术家 / 流派 / 歌手**（2 列，末行「歌手」单独左格）。新增本地音乐歌单页、歌手宫格页。
- 改动文件：
  - `assets/library.png`、`assets/playlist-local.png`、`assets/singer-list.png`
  - `index.html`、`app.js`
  - `README.md`、`IA-UPDATE-2026-09-18.md`、本文件 §3.3 / §4 / §9 / §11
- 与交接一致 / 有出入：原「艺人」拆成「艺术家」（直达艺人详情）与「歌手」（先宫格再详情）。「最近」入口删除；曲库下方「最近播放」横滑条仍在。
- 请小杨确认：
  1. 「艺术家」与「歌手」是否要并存，还是只留其一。
  2. 末行「歌手」单独一格是否改为通栏。

#### 2026-09-18 13:28 · Cursor 接替 UI（曲库 3 列 + 图标）
- 做了：保留「歌手」，去掉独立「艺术家」。曲库入口改为 **3 列**（歌曲 / 我喜欢的 / 本地音乐 / 专辑 / 流派 / 歌手），每格 SF Symbol 在上、文案同一字号在下。
- 改动文件：`assets/library.png`、`app.js`、`index.html`、`README.md`、`IA-UPDATE-2026-09-18.md`、本文件 §3.3 / §4 / §9 / §11
- 图标：`music.note` / `heart.fill` / `iphone` / `square.stack.fill` / `guitars` / `person.fill`
- 请小杨确认：图标是否要换成线框（regular）而不是当前的 medium 填充。

#### 2026-09-18 13:52 · Cursor 接替 UI（我的歌单）
- 做了：曲库下方区块标题「播放列表」改为「我的歌单」，字号与原标题对齐。列表内容和点击不变。
- 改动文件：`assets/library.png`、`app.js`（热区名）、本文件 §3.3 / §9 / §11

#### 2026-09-18 14:00 · Cursor 接替 UI（本地音乐占用空间）
- 做了：本地音乐页标题下，在「本机 · 24 首」之后增加一行「占用 198 MB」（示意总量，字号与副行一致）。
- 改动文件：`assets/playlist-local.png`、本文件 §3.3 / §9 / §11
- 请小杨确认：占用空间是单独一行（当前），还是并进「本机 · 24 首 · 198 MB」。

#### 2026-09-18 14:06 · Cursor 接替 UI（去掉重复曲目数）
- 做了：本地音乐标题下去掉「本机 · 24 首」，只留「占用 198 MB」。「共 24 首」仍在全部播放一行。
- 改动文件：`assets/playlist-local.png`、本文件 §3.3 / §9 / §11

#### 2026-09-18 14:10 · Cursor 接替 UI（歌曲 / 我喜欢的去重曲目数）
- 做了：全部歌曲去掉「曲库 · 1,024 首」，我喜欢的去掉「智能歌单 · 128 首」。曲目数只留在「全部播放（共 24 首）」。
- 改动文件：`assets/playlist-all-songs.png`、`assets/playlist-liked.png`、本文件 §3.3 / §9 / §11

#### 2026-09-18 14:40 · Cursor（HTML 分页原型）
- 做了：停用 PNG 贴图 + 热区。每个屏幕单独一个 HTML，点击在页面之间跳转。入口改为 `index.html` 目录，主路径从 `pages/splash.html` 开始。
- 改动文件：`index.html`、`shared.css`、`shared.js`、`pages/*.html`（28 页）、`README.md`、本文件 §5 / §9 / §11。`app.js` / `style.css` / `assets/` 保留作旧稿对照，页面不再引用。
- 与交接一致 / 有出入：信息架构与跳转保持原样（启动必须经连接；曲库 3 列六宫格；底栏搜索 / 曲库 / 设置；详情无底栏；播放器三态 + 队列 + ⋯）。视觉是同一套深色界面的 HTML 重绘，不是逐像素贴图。全部播放与可见列表对齐为「共 6 首」。
- 请小杨确认：HTML 页是否作为之后改文案/布局的唯一原型，PNG 是否可以冻结。


#### 2026-09-18 · 小杨 · 设置可审稿（对齐产品五块）
- 更新 `pages/settings.html`：服务器卡片+断开/重连；播放仅循环模式（列表/单曲，无音量行）；缓存已用+清除；外观仅跟随系统；关于入口（Logo E / Slogan / 版本在 about）。
- 迷你条 + 底栏三 Tab 保留。通断细则仍跟 A/B/C。
- 路径：`/Users/fuchenlin/Documents/tempwork/yinxia-prototype/pages/settings.html`


#### 2026-09-19 09:45 · 小杨复查 Cursor 更新
- 看了：README、pages/library|settings|splash|connect|daily、§11 既有回写
- Cursor 已落地（原型侧，可继续用）：
  1. HTML 分页原型为主路径（PNG 仅对照）
  2. 曲库 3×2：歌曲 / 我喜欢的 / 本地音乐 / 专辑 / 流派 / 歌手；「我的歌单」+「最近播放」横滑
  3. 底栏三 Tab：搜索 / 曲库 / 设置
  4. 流派 2 列大方块；本地音乐占用示意；歌单曲目数去重
  5. 新增 `pages/daily.html`（每日推荐，从搜索侧进入）
- 与 CR-003 / 产品已锁项的偏差（需改或盖章）：
  1. **设置缺「服务器」块**：当前 settings 只有播放/缓存/外观/关于，缺服务器卡片+断开/重连（产品五块已锁）
  2. **README 写「已添加资料库：启动页 → 曲库」**：与已锁「禁启动直达曲库」冲突；冷启动仍应走选服务→连接（已登录态可另议，但文档不能写成直达）
  3. **每日推荐**：产品未锁发现流；先当搜索实验页，不进 MVP 关键页签收，除非产品加锁
- 请用户/产品确认（小杨默认建议）：
  1. HTML 为唯一改稿源，PNG 冻结 → **建议是**
  2. 六宫格图标保持线框 stroke（当前 library 多为线框）→ **建议是**
  3. 流派大方块先纯色，纹理后置 → **建议是**
  4. 设置立刻补回服务器卡片 → **建议是（我可直接改）**


#### 2026-09-19 09:50 · 小杨 · 按用户拍板修正
- 用户选择：按建议改（补服务器 + 修正启动）
- 已改：
  1. `pages/settings.html` 补回服务器卡片 + 重连/断开（通断细则仍跟 A/B/C）
  2. `README.md` 主路径改为强制 `启动→选服务→连接→成功→曲库`，删除「已添加可直达」写法；每日推荐标为实验页
  3. `pages/splash.html` 去掉直达曲库链接（若有）
- 每日推荐：`pages/daily.html` 保留但不进 MVP 关键页签收


#### 2026-09-19 10:05 · 小杨 · 设计细项盖章 + 空/弱网骨架
- 用户说「继续」→ 盖章未锁设计细项，见 `DESIGN-STAMPS.md`
- 已盖：HTML 唯一源；宫格线框图标；流派纯色大方块；每日推荐为实验页
- 原型：`library.html`「我喜欢的」心形改为线框；新增 `empty-search.html`、`weak-network.html`
- 仍等产品：A/B/C 通断；三级详情/空弱网完整矩阵签字


#### 2026-09-19 10:10 · 小杨 · 三级详情一页纸
- 新增 `DETAIL-SPEC.md`：专辑/歌单/歌手共性壳、头图、曲目行、⋯ 面板、验收点
- 关键规则：进页藏 Tab+迷你条；全部播放=追加；专辑页副行省略专辑名；心形/星仅在 ⋯


#### 2026-09-19 10:52 · 小杨 → 小戴
- 用户指示：把三级详情规格给小戴，开始工程实现
- 已 SendToAgent 小戴：DETAIL-SPEC.md + DESIGN-STAMPS.md + 开工顺序（三 Tab、启动链路、六宫格、详情、设置五块）
- 仍排除：A/B/C、完整空弱网、每日推荐、本地完整导入


#### 2026-09-19 18:55 · 小杨 → 小戴
- 用户反馈连接页像系统 Form、与原型不符；小戴要改 UI
- 已出盖章：`CONNECT-SPEC.md`（选服务 + 连接表单布局/字号/色/与 Form 差异）
- 稿源：`pages/connect.html` / `connect-form.html` / `connect-fail.html` / `shared.css`
