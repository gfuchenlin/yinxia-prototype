# 音匣 · 三级详情规格（一页纸）

**状态：** 设计盖章 · 2026-09-19 · 小杨  
**适用范围：** 专辑详情 / 歌单详情（含我喜欢的、全部歌曲、流派歌单、本地音乐）/ 歌手详情  
**原型对照：** `pages/album.html` · `pages/playlist*.html` · `pages/artist-*.html`  
**实现优先级：** 给小戴竖屏主路径；图标一律 SF Symbol

---

## 0. 共性壳（所有三级页）

| 项 | 规格 |
|---|---|
| 进入 | Push；**隐藏底栏三 Tab + 迷你播放条**；返回后恢复 |
| 顶栏 | 左返回 · 中标题（专辑/歌单/歌手语境短标题）· 右 `⋯`（页级更多，MVP 可先 toast） |
| 安全区 | 顶随刘海；底仅 Home Indicator，无 Tab |
| 横屏 | MVP 可同结构左右压缩；精细横屏分栏不挡主路径 |
| 空/错 | 列表空：「暂无歌曲」；加载失败：重试按钮（弱网细则跟产品矩阵） |

---

## 1. 头图区（专辑 / 歌单共用）

```
┌ 返回          标题          ⋯ ┐
│ [封面 112pt]  主标题（最多 2 行） │
│ 圆角 12       副标题 1 行         │
│               （元信息）          │
├ 全部播放（共 N 首）  主色文字按钮 ─┤
├ [页内搜索…………]              排序 ┤
└ 曲目列表 …………………………… ┘
```

| 元素 | 规格 |
|---|---|
| 封面 | **112×112 pt**，圆角 **12**；无封面用纯色占位 |
| 背景 | 可选封面取色模糊铺底（alpha 低）；无图则用默认深底 |
| 主标题 | 20–22 pt Semibold/Medium，最多 2 行截断 |
| 副标题 | 13 pt 次要色 |
| 全部播放 | 文案 **`全部播放（共 N 首）`**；点击 = **追加整表到队列并开始播第一首**（不替换，CR-003） |
| 页内搜索 | 过滤**当前列表**；占位「搜索本专辑/本歌单歌曲」 |
| 排序 | MVP 可先入口占位；默认列表顺序 |

### 副标题文案

| 类型 | 副标题 |
|---|---|
| 专辑 | `年份 · 歌手名`（无年份则只歌手） |
| 普通歌单 | `服务器歌单` 或 `N 首`（不要和「全部播放」重复堆两处大数字；曲目数以全部播放为准） |
| 我喜欢的 | `智能歌单`（不要再写「128 首」在副标题） |
| 全部歌曲 | `曲库` |
| 流派歌单 | `流派 · Pop` 这类 |
| 本地音乐 | **仅** `占用 xxx MB`（曲目数只在全部播放行） |

---

## 2. 曲目行（统一）

```
[序号]  歌名
        [格式徽标] 体积 · 歌手 · 专辑     […]
```

| 项 | 规格 |
|---|---|
| 行高 | **56 pt**（正在播可 更高，详见队列稿） |
| 序号 | 2 位，次要色；播放中可换成等响图标（MVP 可先高亮歌名） |
| 歌名 | 1 行主色 |
| 副行 | **`格式徽标` + `体积 · 歌手 · 专辑`** |
| 格式徽标 | 小描边胶囊：`flac` / `mp3` / `aac`… |
| 专辑页特例 | 副行 **省略专辑名** → `格式 · 体积 · 歌手` |
| 歌手页·歌曲 Tab | 副行保留专辑名 |
| 点击行 | 追加该曲到队列并播放 |
| 行尾 `⋯` | 打开歌曲操作面板（**不要**在行上直接露心形/星级） |

### 歌曲 `⋯` 面板（竖屏底部 sheet）

- 头：封面 + 歌名 + 歌手；心形喜欢；1–5 星（仅此面板）
- MVP：下一首播放 · 追加到队列 · 查看歌手 · 查看专辑  
- 后续灰显：下载、定时、倍速等

---

## 3. 歌手详情（结构不同）

```
┌ 返回                         ♡ ┐
│     歌手名（居中大标题）         │
│     N 专辑 · M 歌曲             │
│     来自已连接曲库               │
├  专辑  │  歌曲  │  相似         ┤
└（分段内容）─────────────────────┘
```

| Tab | 内容 |
|---|---|
| 专辑 | 网格封面；点进专辑详情 |
| 歌曲 | 与歌单列表同规：全部播放（共 N 首）+ 页内搜索 + 曲目行（含专辑名） |
| 相似 | MVP：**保留入口 + 空态**「暂无相似歌手」（是否接数据待产品） |

- 顶栏心形：喜欢/取消喜欢该歌手（服务端能力没有则可隐藏）  
- **无**底栏、**无**迷你条（同共性壳）

---

## 4. 与一级/二级差异（防做错）

| | 曲库首页 | 三级详情 |
|---|---|---|
| 底栏 / 迷你条 | 有 | **无** |
| 服务器卡片 | 有 | 无 |
| 六宫格 | 有 | 无 |
| 全部播放 | 无 | 有（追加） |

---

## 5. 验收要点（给小李 / 小戴）

1. 任意三级页：无 Tab、无迷你条；返回后两者回来  
2. 全部播放文案含 **共 N 首**，且 N = 当前列表数；行为 = 追加  
3. 专辑内曲目副行不出现专辑名；歌单/歌手歌曲 Tab 出现专辑名  
4. 行上无心形/星；只在 `⋯` 里  
5. 歌手三 Tab 可切；相似允许空态  
6. 本地音乐头区只有占用空间，不重复「共 N 首」在副标题  

---

## 6. 开放项（不挡本页落地）

- 页级 `⋯` 菜单具体项  
- 排序规则  
- 相似歌手是否进 MVP 数据  
- 通断 A/B/C、弱网矩阵（另纸）
